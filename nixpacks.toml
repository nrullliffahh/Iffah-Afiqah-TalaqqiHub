# Dockerfile build only — Java Servlet/JSP on Tomcat (NOT Node.js).
# Kerocket: use kerocket.toml + Dockerfile; do not run Nixpacks language auto-detection.

[build]
dockerfile = "Dockerfile"

[variables]
NIXPACKS_NO_NODE = "1"
NIXPACKS_NO_PYTHON = "1"
NIXPACKS_NO_RUBY = "1"
