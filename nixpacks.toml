# This project is a Java Servlet/JSP Maven WAR app on Tomcat — not Node.js.
# Kerocket must use the Dockerfile build.

[build]
dockerfile = "Dockerfile"

[start]
cmd = "/usr/local/bin/docker-entrypoint.sh catalina.sh run"

[variables]
NIXPACKS_NO_NODE = "1"
