# This project is a Java Servlet/JSP Maven WAR app.
# Kerocket should use the Dockerfile build (recommended for Tomcat deployments).

[build]
dockerfile = "Dockerfile"

[start]
cmd = "/usr/local/bin/docker-entrypoint.sh catalina.sh run"
